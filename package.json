{
  "name": "titanic-iceberg-run",
  "version": "0.1.0",
  "description": "Titanic: Iceberg Run - captain the Titanic across the North Atlantic and avoid colliding with icebergs. Built with Three.js.",
  "type": "module",
  "module": "src/main.ts",
  "packageManager": "bun@1.3.3",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "three": "^0.182.0"
  },
  "devDependencies": {
    "@types/three": "^0.182.0",
    "typescript": "^5.9.3",
    "vite": "^7.3.0"
  }
}
